[package]
name        = "lex-soft"
version     = "0.2.0"
description = "LLM-agent platform — registry, relationship graph, resolver, runner."
license     = "EUPL-1.2"

[dependencies]
lex-schema = { path = "../../generic/lex-schema" }
lex-web    = { path = "../../generic/lex-web" }
lex-log    = { path = "../../generic/lex-log" }
lex-cli    = { path = "../../generic/lex-cli" }
lex-jobs   = { path = "../../generic/lex-jobs" }
lex-spec   = { path = "../lex-spec" }
lex-llm    = { path = "../lex-llm" }
lex-agent  = { path = "../lex-agent" }
lex-mcp    = { path = "../lex-mcp" }
